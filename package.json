{
  "private": true,
  "scripts": {
    "prebuild": "rm -rf dist .cache .parcel-cache",
    "build:tailwind": "tailwindcss build -i src/styles.css -c ./tailwind.config.js -o distribution/styles.css",
    "build": "parcel build src/manifest.json --no-content-hash --no-source-maps --dist-dir distribution --no-cache --detailed-report 0 && npm run build:tailwind",
    "lint": "run-p lint:*",
    "lint-fix": "run-p 'lint:* -- --fix'",
    "lint:js": "xo",
    "test": "run-p lint:* build",
    "watch": "npm run build:tailwind && parcel watch src/manifest.json --dist-dir distribution --no-cache --no-hmr"
  },
  "browserslist": [
    "last 1 Chrome version",
    "last 1 Firefox version"
  ],
  "xo": {
    "semicolon": false,
    "space": true,
    "envs": [
      "browser"
    ],
    "rules": {
      "no-unused-vars": [
        "error",
        {
          "varsIgnorePattern": "browser"
        }
      ],
      "unicorn/prefer-module": "off",
      "no-trailing-spaces": "warn",
      "capitalized-comments": "off"
    }
  },
  "dependencies": {
    "axios": "^0.24.0",
    "lodash": "^4.17.21",
    "postcss": "^8.4.5",
    "tailwindcss": "^2.2.19",
    "webext-options-sync": "^2.0.1",
    "webextension-polyfill": "^0.8.0"
  },
  "devDependencies": {
    "@parcel/config-webextension": "^2.0.0-rc.0",
    "@parcel/transformer-image": "^2.0.0-rc.0",
    "autoprefixer": "^10.4.1",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.0.0-rc.0",
    "postcss-import": "^14.0.2",
    "xo": "^0.44.0"
  },
  "webExt": {
    "sourceDir": "distribution"
  }
}
