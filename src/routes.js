const routes = Object.freeze({
  signin: '/login',
  dashboard: '/dashboard',
  project_settings: '/projects/settings/:id',
  project: '/projects/:id',
  contact: '/contact',
})

export default routes
